<div class="containertoo">
  <div class="row justify-content-center">
    <div class="col-sm-6">
      <div id="bob">
        <%= simple_form_for([@cosplay, @booking]) do  |f| %>
          <%= f.input :date %>
          <%= f.button :submit %>
        <% end %>
      </div>
    </div>
  </div>
</div>
